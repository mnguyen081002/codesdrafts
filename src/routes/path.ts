export const PATH_DASHBOARD = {
  main: '/',
  home: '/home',
  PAYMENT: 'payment',
};

export const PATH_AUTH = {
  login: '/login',
  register: '/register',
};

export enum ADMIN_PATH {
  DASHBOARD = 'dashboard',
  SETTING = 'setting',
  COURSES = 'courses',
}
