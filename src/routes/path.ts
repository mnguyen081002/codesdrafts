export const PATH_DASHBOARD = {
  main: '/',
  home: '/home',
};

export const PATH_AUTH = {
  login: '/login',
  register: '/register',
};

export enum ADMIN_PATH {
  DASHBOARD = 'dashboard',
  SETTINGS = 'settings',
}
